{% extends 'base.html.twig' %}

{% set title = 'trip.edit.label'|trans %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <h1>{{ title }}</h1>

    {{ include('trip/_form.html.twig', {button_label: 'update.label'}) }}

    <twig:Button:Trip:BackToIndex />

    {{ include('trip/_delete_form.html.twig') }}
{% endblock %}

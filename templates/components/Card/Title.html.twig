{# @block content The title text of the card #}
<div
    class="{{ ('app-card-title ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</div>

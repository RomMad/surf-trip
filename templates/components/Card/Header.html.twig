{# @block content The header area, typically contains `Card:Title` and `Card:Description` #}
<div
    class="{{ ('app-card-header ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</div>

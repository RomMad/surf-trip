{# @block content The main content area of the card #}
<div
    class="{{ ('app-card-content ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</div>

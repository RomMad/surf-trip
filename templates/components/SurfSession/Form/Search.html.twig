{% props form %}

{{ form_start(form, {attr: {
    'data-turbo-frame': constant('App\\Turbo\\Frame\\SurfSessionFrameId::RESULTS'),
}}) }}
    <twig:Form:Query :query="form.query" />
    <twig:Form:Period :period="form.period" />
{{ form_end(form) }}

<twig:Button
    as="a"
    variant="outline"
    size="sm"
    target="_blank"
    href="{{ href }}"
    title="{{ title|trans }}"
>
    <twig:ux:icon name="{{ icon }}" />
    {{ label|trans }}
</twig:Button>

{# @block content The grouped avatars, typically multiple `Avatar` components #}
<div
    data-slot="avatar-group"
    class="{{ ('app-avatar-group group/avatar-group ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</div>

{# @block content The count indicator showing number of additional avatars (e.g., "+3") #}
<div
    data-slot="avatar-group-count"
    class="{{ ('app-avatar-group-count ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</div>

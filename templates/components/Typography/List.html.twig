{# @block content The list items (`<li>`). #}
<ul
    class="{{ ('app-list ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'typography-list',
    }) }}
>
    {%- block content %}{% endblock -%}
</ul>

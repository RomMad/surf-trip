{# @block content The heading content. #}
<h4
    class="{{ ('app-h4 ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'typography-h4',
    }) }}
>
    {%- block content %}{% endblock -%}
</h4>

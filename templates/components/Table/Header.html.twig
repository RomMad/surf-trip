{# @block content The default block #}
<thead
    class="{{ 'app-table-header ' ~ attributes.render('class')|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</thead>

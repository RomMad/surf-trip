{# @block content The default block #}
<tfoot
    class="{{ 'app-table-footer ' ~ attributes.render('class')|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</tfoot>

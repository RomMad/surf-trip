{%- props locationField -%}

<div
    data-controller="location-autocomplete"
    data-location-autocomplete-api-key-value="{{ mapbox_public_api_key }}"
    data-location-autocomplete-no-results-text-value="{{ 'location.no_results'|trans }}"
>
    <div class="app-form-grid-2">
        {{ form_rest(locationField) }}
    </div>
</div>

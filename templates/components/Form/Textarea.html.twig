{# @block content The default block #}
<textarea
    class="{{ 'app-textarea ' ~ attributes.render('class')|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</textarea>
